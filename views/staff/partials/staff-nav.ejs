<nav class="fixed top-0 left-0 w-full z-40 bg-gray-100 shadow px-6 py-4 flex justify-between items-center">
  <!-- Navigation Links -->
  <div class="flex space-x-2">
    <a href="/staff/dashboard" class="px-5 py-2 rounded-md bg-white text-black font-medium shadow">Home</a>
    <a href="/api/staff/orders" class="px-5 py-2 rounded-md bg-white text-black font-medium shadow">My Orders</a>
    <a href="/api/staff/menu" class="px-5 py-2 rounded-md bg-white text-black font-medium shadow">Menu</a>
  </div>

  <!-- Dashboard Title -->
  <h1 class="text-3xl font-bold text-[#2980B9] text-center flex-grow ml-6">Staff Dashboard</h1>

  <!-- Right Side: Search & Logout -->
  <div class="flex items-center space-x-4">
    <!-- Search (optional) -->
    <form action="#" method="GET" class="flex items-center space-x-2">
      <input
        type="text"
        name="q"
        placeholder="Search"
        required
        class="px-4 py-2 border border-gray-300 rounded-full focus:outline-none focus:ring-1 focus:ring-green-500"
      />
      <button
        type="submit"
        class="px-4 py-2 border border-green-500 text-green-600 rounded-full hover:bg-green-50 transition font-medium"
      >
        Search
      </button>
    </form>

    <!-- Logout Button -->
    <form action="/staff/logout" method="POST">
      <button
        type="submit"
        class="px-5 py-2 bg-red-500 text-white rounded-full font-medium hover:bg-red-600 transition shadow"
      >
        Logout
      </button>
    </form>
  </div>
</nav>
