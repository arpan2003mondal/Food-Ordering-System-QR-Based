<nav class="fixed top-0 left-0 w-full z-40 bg-gradient-to-r from-indigo-100 to-blue-100 shadow-md px-4 py-3  border-b border-blue-300">
  <div class="flex flex-col sm:flex-row sm:justify-between items-center sm:space-y-0 space-y-3">

    <!-- Center: Dashboard Title -->
    <h1 class="text-3xl sm:text-3xl font-bold text-[#1C3D5A] text-center">
      Staff Dashboard
    </h1>

     <!-- Right Side: Nav Buttons & Logout -->
  <div class="flex items-center space-x-2">
    <!-- Navigation Buttons -->
    <a href="/staff/dashboard" class="px-4 py-2 rounded-full bg-blue-500 text-white font-semibold text-sm shadow hover:bg-blue-600 transition">
      Live Orders
    </a>
    <a href="/staff/past-orders" class="px-4 py-2 rounded-full bg-purple-500 text-white font-semibold text-sm shadow hover:bg-purple-600 transition">
      Past Orders
    </a>

    <!-- Logout Button -->
    <form action="/staff/logout" method="POST">
      <button
        type="submit"
        class="px-4 py-2 bg-red-500 text-white rounded-full font-semibold text-sm hover:bg-red-600 transition shadow"
      >
        Logout
      </button>
    </form>
  </div>

  </div>
</nav>
